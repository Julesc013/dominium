# AIDE Import Templates

This directory contains portable import templates used when building the local
AIDE Lite export pack. These templates are source-controlled input files, not
target import results.

Imports must preserve target-local memory and state, avoid raw prompt or raw
response storage, and run in dry-run or explicitly reviewed modes only.
