# Q50 Readiness

## Status

Readiness status: `READY_FOR_Q50_WITH_WARNINGS`.

## Recommendation

- Q50 should be an upgrade dry-run, not a first install.
- Use source bundle: `C:/Inbox/Git Repos/aide/.aide/release/dist/aide-lite-pack-v0.zip` or equivalent tar.gz from the same dist directory.
- Use Q50 title: `Q50 Dominium Fresh Install / Upgrade from Stable AIDE Pack`.
- The latest task packet was generated for Q50 at `.aide/context/latest-task-packet.md`.

## Not Blocked

- Repo identity is confirmed as Dominium.
- Starting worktree was clean.
- Existing target AIDE is present and passes core validation.
- `.aide.local/` is ignored and untracked.
- Release bundle artifacts are present.
- Release bundle checksums passed.
- Archive listing/forbidden path scan passed.

## Warnings

- Target AIDE is older (`q24.existing-tool-adapter-compiler.v0`) and lacks Q36-Q48 install/upgrade/repair/rollback/uninstall planning command families.
- Release bundle is a local preview/no-publish bundle with `apply_mode_available: false`.
- Existing target memory includes stale source/import paths and should be updated only with preservation-aware logic.
- Dominium gate systems can write audit/cache/build artifacts and were not run in Q49.
- `aide_lite.py verify` reported stale review-packet reference warnings before review-pack regeneration.
- `changelog preview` reported malformed commits in recent history.

## Allowed Paths For Q50 Dry-Run

- `.aide/queue/<Q50-id>/**`.
- `.aide/reports/**` compact Q50 planning and comparison reports.
- `.aide/context/latest-task-packet.md` and `.aide/context/latest-review-packet.md` if generated by installed AIDE.
- Existing safe generated AIDE outputs such as `.aide/git/latest-helper-plan.*`, `.aide/changelog/*`, `.aide/evals/runs/latest-golden-tasks.*`, `.aide/repo/**`, `.aide/roots/**`, `.aide/tools/**` only if Q50 explicitly permits and the command is non-mutating outside AIDE evidence.
- Temporary extraction/review directories outside Dominium.

## Forbidden Paths For Q50

- Product/source/doctrine/tool roots unless the Q50 prompt explicitly expands scope: `runtime/**`, `engine/**`, `game/**`, `apps/**`, `content/**`, `contracts/**`, `specs/**`, `data/**`, `docs/**`, `tools/**`, `scripts/**`, `cmake/**`, `.github/**`.
- `.git/**`, `.aide.local/**`, `.dominium.local/**`, ignored build/dist/out/cache directories.
- Release, security, update, branch, tag, CI, remote, publish, and active hook state.

## Validation Q50 Must Run

- `git status --short`, `git diff --check`, `git check-ignore .aide.local/`.
- Existing AIDE `doctor`, `validate`, `test`, `selftest`, `eval run`, `verify`, `review-pack`.
- Release bundle checksum and archive listing checks.
- Targeted secret scan with redaction/classification.
- Any Dominium gate/TestX/AuditX/RepoX validation only if Q50 explicitly scopes it and records generated outputs.

## Q50 Expected Output

- Upgrade dry-run comparison report.
- Preservation/conflict matrix.
- Files-to-keep/files-to-add/files-to-update/files-to-skip plan.
- Absorption/wrapper plan for existing tool systems.
- Validation evidence and review status, not an applied install.
