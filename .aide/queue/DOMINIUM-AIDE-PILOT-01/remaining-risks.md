# Remaining Risks

- First Dominium import only; target-specific repeatability is not yet proven.
- Golden tasks are generic AIDE Lite checks, not Dominium doctrine-specific
  acceptance tests.
- Compact doctrine refs may be insufficient for broad implementation prompts
  without manual task-specific curation.
- The verifier WARN requires reviewer awareness of cache metadata, Q23 evidence
  queue scope, and pre-existing dirty FAST validation report files.
- `.aide/context/repo-map.json` and `test-map.json` are large metadata files;
  use compact packets for prompts, not raw generated maps.
- No provider routing, live Gateway, exact tokenizer, billing ledger, model
  review, autonomous loop, semantic cache, vector DB, MCP, or A2A exists.
- Pack import may need another pass after Q23 review to generalize
  doctrine-heavy repo support without target-local script adaptation.
