# Exec Plan

Status: needs_review

1. Confirm the remaining DCHECK/Q53 blocker.
2. Repair AIDE review packet generation so nested selftest repositories cannot inherit the outer Dominium Git repo.
3. Add a modular AIDE-side XStack command family and generated wrapper contract.
4. Validate AIDE core, XStack integration, tool validation, Git hygiene, and secret scan.
5. Attempt a scoped commit if Git index writes are available.

No product, doctrine, legacy tool, root, branch, release, provider, or network work is authorized by this repair.
