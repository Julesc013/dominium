# AIDE-MOVE-02-REFINE Blockers

## Candidate Blockers

- No preferred-root docs-only or evidence-only file remains.
- `ide/manifests/**` remains machine-readable projection metadata and is explicitly deferred.
- `performance`, `validation`, `governance`, and `meta` contain active Python/tooling or policy/governance material.
- `templates/adapter_template.md` and `templates/domain_contract_template.md` are template scaffolds with `convert` fates, not low-risk move candidates.
- Data, pack, bundle, spec, update, and library docs-like files are identity-, authority-, update-, or build-sensitive.

## Evidence Gaps

No required AIDE-MOVE-02 evidence was missing. The blocker is candidate safety, not missing evidence.

## Move Application Status

Move application remains unauthorized. No `AIDE-GATE-04` or `AIDE-MOVE-02-APPLY` should start from this result.

## Deferred Broad Roots

High-risk and protected root families remain deferred until dedicated validators, reference plans, and ownership reviews exist.
