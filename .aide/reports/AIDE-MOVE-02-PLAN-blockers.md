# AIDE-MOVE-02-PLAN Blockers

## Blocking Issues For Apply

- No second move candidate was selected.
- No move application is authorized.
- Remaining `ide/` material is machine-readable projection metadata under `ide/manifests/**` and remains deferred.
- Preferred remaining roots contain active Python/tooling modules rather than docs-only or evidence-only files.
- Reference complexity for `performance/`, `validation`, `governance`, and `meta` is too high for a second low-risk move without refinement.

## High-Risk Exclusions

- `core/`, `control`, `net`, `lib`, `libs`, `data`, `packs`, `profiles`, `bundles`, `compat`, `locks`, `repo`, `safety`, `security`, `specs`, and `updates` remain excluded from second-wave planning.

## Missing Evidence

No missing AIDE-GATE-03 or AIDE-MOVE-01 evidence blocked this task. The blocker is candidate quality: no safe docs-only/evidence-only candidate remains in the preferred roots.

## Reference Risks

No references are authorized for rewrite. Future refinement must separate active tool/source references from historical and generated evidence before selecting another candidate.

## Validation Risks

Full eval, full CTest, CMake configure/build, package generation, release generation, and product binaries remain out of scope for this planning task.

## Move Application Status

Move application remains unauthorized.
