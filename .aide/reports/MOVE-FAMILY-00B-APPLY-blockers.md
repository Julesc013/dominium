# MOVE-FAMILY-00B-APPLY Blockers

## Blocking Issues

None.

## Warnings

- Strict validators emit known TOML fallback-parser warnings while passing.
- Historical/audit/planning references to old `ide/manifests/**` paths remain by design.
- Generated-output references to `ide/manifests/*.projection.json` remain in generator and registry surfaces.
- Focused RepoX initially failed on missing four-line status headers in three touched root-recycling docs; metadata-only headers were repaired and the focused check passed on rerun.
- Full CTest, full eval, CMake configure/build, product binary execution, package/release generation, portable projection regeneration, and internal pilot release regeneration were not run by scope.

## Authorization

The MOVE-FAMILY-00B-GATE authorization has been consumed. No other move wave is authorized by this apply task.

## Follow-Up

`MOVE-FAMILY-00B-PROOF - IDE Root Retirement Proof`.
