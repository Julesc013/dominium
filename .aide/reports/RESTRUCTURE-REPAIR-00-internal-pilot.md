# RESTRUCTURE-REPAIR-00 Internal Pilot

Command:

`python tools/validators/check_internal_pilot_release.py --repo-root . --release-root .dominium.local/releases/internal-pilot-0 --json --strict`

Result: PASS.

The validator reported no blockers or warnings, 4,718 checksum entries, ignored release staging, and all five product binaries present.
