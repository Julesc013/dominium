# AIDE-MOVE-01-APPLY Blockers

## Blockers

None.

## Warnings

- Local `origin/main` already matched the AIDE-GATE-02 commit instead of the older SHA expected by the prompt. No remote mutation was performed during this task.
- Strict validators run through `python` passed with non-blocking `tomllib` fallback warnings.
- Strict layout validators regenerated `tools/migration/root_inventory.json` and `tools/migration/root_move_map.json` headers only. Those timestamp/SHA-only metadata changes did not reflect the applied move and were not kept.
- Remaining `ide/README.md` references are historical, generated review items, or root-recycling planning evidence. They were not part of the six apply-phase rewrites.

## Deferred Material

- `ide/manifests/**` remains deferred and untouched.
- Generated architecture registry/graph references remain review/regeneration candidates for a later dedicated metadata task.

## No Other Moves

No files other than `ide/README.md` were moved. No files were deleted, no shims or aliases were created, and no layout exception was retired.
