# AIDE Model Routing Metadata

This directory contains metadata-only routing profiles, provider capability
descriptions, hard floors, and fallback rules for AIDE Lite.

The files are advisory and repo-local. They do not enable provider calls,
model calls, network calls, Gateway forwarding, or automatic route mutation.
