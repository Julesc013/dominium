{
  "schema_version": "dominium.move_family_00c_a.validation_plan.v1",
  "task_id": "MOVE-FAMILY-00C-A-PLAN",
  "status": "draft",
  "approval_status": "not_approved",
  "apply_allowed": false,
  "planning_validation": [
    "py -3 .aide/scripts/aide_lite.py doctor",
    "py -3 .aide/scripts/aide_lite.py validate",
    "py -3 .aide/scripts/aide_lite.py test",
    "py -3 .aide/scripts/aide_lite.py selftest",
    "py -3 .aide/scripts/aide_lite.py tools validate",
    "py -3 .aide/scripts/aide_lite.py roots validate",
    "py -3 .aide/scripts/aide_lite.py repo validate",
    "py -3 .aide/scripts/aide_lite.py commit check --latest",
    "parse MOVE-FAMILY-00C-A JSON artifacts",
    "parse MOVE-FAMILY-00C-A TOML plan",
    "python tools/validators/check_repo_layout.py --repo-root . --strict",
    "python tools/validators/check_root_allowlist.py --repo-root . --strict",
    "python tools/validators/check_distribution_layout.py --repo-root . --strict",
    "python tools/validators/check_component_matrices.py --repo-root . --strict",
    "python scripts/verify_docs_sanity.py --repo-root .",
    "python scripts/verify_build_target_boundaries.py --repo-root .",
    "python scripts/verify_ui_shell_purity.py --repo-root .",
    "python scripts/verify_abi_boundaries.py --repo-root .",
    "git diff --check",
    "git diff --cached --check if staged"
  ],
  "required_after_future_apply": [
    "AIDE doctor/validate/test/selftest",
    "AIDE tools validate",
    "AIDE roots validate",
    "AIDE repo validate",
    "AIDE commit check --latest",
    "strict repo layout validator",
    "strict root allowlist validator",
    "strict distribution layout validator",
    "strict component matrix validator",
    "docs sanity",
    "build target boundaries",
    "UI shell purity",
    "ABI boundaries",
    "focused RepoX",
    "py_compile tools/validators/validation tools/validators/identity tools/validators/stability and shim files",
    "import smoke old imports for shim paths",
    "import smoke new imports for tools.validators.validation tools.validators.identity tools.validators.stability",
    "run or import-smoke affected consumers from the import rewrite plan",
    "run validation FAST through tools/validation/tool_run_validation.py if available",
    "run identity helper smoke for validate_identity_repo and validate_identity_path",
    "run stability helper smoke for validate_all_registries and validate_pack_compat",
    "run legacy-import static check from the static check plan",
    "ctest --preset verify -R inv_repox_rules --output-on-failure",
    "ctest --preset verify -L smoke --output-on-failure --timeout 300 if active validator/AppShell imports change"
  ],
  "not_required_for_planning": [
    "full CTest",
    "full eval",
    "CMake configure/build",
    "package/release generation",
    "product binaries"
  ],
  "no_apply": true
}
