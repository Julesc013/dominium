# Fixture: 10k Systems Latent

Large latent universe with a single active region. Used to guard against
global-iteration regressions and to validate latent scaling.

Files:
- `fixture.cfg` defines modules, budgets, and expected hashes.
