#!/bin/sh
# Dominium portable entrypoint: runs the launcher with --home set to the bundle root.

SELF="$0"
while [ -L "$SELF" ]; do SELF="$(readlink "$SELF")"; done
BIN="$(cd "$(dirname "$SELF")" && pwd)"
DOM_HOME="$(cd "$BIN/.." && pwd)"

exec "$BIN/@DOM_LAUNCHER_BINARY@" --home="$DOM_HOME" "$@"
