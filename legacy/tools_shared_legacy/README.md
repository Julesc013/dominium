# Legacy Tools Shared Runtime

These shared tool runtime sources were moved out of `tools/` while the canonical
product boundaries and missing runtime dependencies are resolved. The active
build uses a stubbed tools shared runtime until game/runtime and dominium
headers are wired for the full tool harness.
