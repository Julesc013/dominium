# Legacy Tools CMake

This folder contains the previous `tools/CMakeLists.txt` content, preserved while
the build graph is refactored to match the canonical product layout and boundary
rules. The legacy file references paths and targets that no longer exist, so it
is not used by default.
