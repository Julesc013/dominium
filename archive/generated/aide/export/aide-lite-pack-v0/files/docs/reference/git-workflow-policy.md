# AIDE Git Workflow Policy

This reference summarizes the portable Git workflow policy. The canonical
policy is `.aide/policies/git-workflow.yaml`.

`main` is the canonical branch, integration branches are not canonical truth,
and helper plans are dry-run by default. Remote mutation, force pushes, branch
deletion, and implicit merge behavior require explicit reviewed authorization.

This reference is advisory and does not execute Git commands.
