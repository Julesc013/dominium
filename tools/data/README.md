# Data Import Tools

Deterministic source-to-derived import tools for Prompt 18.

Commands:
- `tools/data/tool_spice_import`
- `tools/data/tool_srtm_import` (added in Phase 3)

Design rules:
- deterministic input ordering
- canonical JSON output
- explicit provenance headers
- no runtime mutation paths

