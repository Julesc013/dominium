# Schema/Protocol Evolution Contract Fixtures

These fixtures exercise `tools/validators/contracts/check_schema_protocol_evolution.py`.

They are test-only examples for SCHEMA-PROTOCOL-LAW-01. They do not promote any
schema or protocol to stable product compatibility. Valid fixtures model the
minimum governed policy shape; invalid fixtures demonstrate forbidden omissions,
silent defaults, missing protocol versions, and missing migration refusal policy.
