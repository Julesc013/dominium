# Test Fixtures Root

Status: PROVISIONAL
Phase: CONVERGE-09

`tests/fixtures/` contains test fixtures and reusable verification inputs.

Fixtures here are not product content unless explicitly duplicated or referenced by content policy. Deterministic ordering, stable hashing, fixed-point truth discipline, and named RNG stream rules apply where fixtures participate in replay, regression, or golden checks.
