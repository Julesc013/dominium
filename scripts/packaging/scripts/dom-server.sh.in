#!/bin/sh
SELF="$0"
while [ -L "$SELF" ]; do SELF="$(readlink "$SELF")"; done
ROOT="$(cd "$(dirname "$SELF")" && pwd)"

: "${DOM_PLATFORM:=POSIX_HEADLESS}"
: "${DOM_RENDER_BACKEND:=SOFTWARE}"
: "${DOM_HEADLESS:=1}"

export DOM_PLATFORM DOM_RENDER_BACKEND DOM_HEADLESS

EXEC="dom_server-posix-headless"

exec "$ROOT/$EXEC" "$@"
