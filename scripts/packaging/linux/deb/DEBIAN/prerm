#!/bin/sh
set -e

INSTALL_DIR="/opt/dominium"
CLI_BIN="$INSTALL_DIR/dominium-setup-cli"

if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    if [ -x "$CLI_BIN" ]; then
        "$CLI_BIN" --scope=system --action=uninstall --dir="$INSTALL_DIR" || true
    fi
fi

exit 0
