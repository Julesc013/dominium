# Launcher Extension API (dom_launcher_api.h)

TODO: fold the existing launcher plugin/extension notes into this file. The public surface is exposed through `include/dominium/dom_launcher_api.h` and must stay stable for external tabs/commands; internal headers remain under `include/dom_priv/dom_launcher`.
