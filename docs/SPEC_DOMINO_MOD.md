# Game Mod API (dom_game_mod_api.h)

TODO: define the native/script modding ABI (package layout, callbacks, sandbox rules). `include/dominium/dom_game_mod_api.h` is currently a stub and must be filled with a deterministic, float-free interface before mods ship.
