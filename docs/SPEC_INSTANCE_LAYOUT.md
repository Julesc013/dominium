# Instance Layout (PROGRAM/DATA/STATE roots)

TODO: capture how PROGRAM_ROOT, DATA_ROOT, and STATE_ROOT are resolved across products. The shared logic should live in the platform/core layer, not scattered per-product. Document expected on-disk layout for installs, instances, saves, and runtime caches.
