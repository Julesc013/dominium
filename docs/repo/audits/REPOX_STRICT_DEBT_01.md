Status: DERIVED
Last Reviewed: 2026-05-20
Supersedes: none
Superseded By: none

# REPOX-STRICT-DEBT-01 Audit

## Scope

Repair the remaining RepoX STRICT proof failures after the canonical cleanup and remediation pass. This was proof repair only: no broad structure refactor, no old roots recreated, and no semantic IDs or pack IDs changed.

## Starting State

- Branch: `main`
- Starting commit: `7e8879e6d0e6b13eebb181d3cd41cef3acb71dfa`
- Previous remediation audit: `docs/repo/audits/CANON_REMEDIATION_FULL_PROOF_01.md`
- Worktree at preflight: clean

## RepoX STRICT Command

```text
python scripts/ci/check_repox_rules.py --repo-root . --profile STRICT --proof-manifest-out .dominium.local/repox-strict-debt-01/repox_proof_manifest_before.json --profile-out .dominium.local/repox-strict-debt-01/repox_profile_before.json
```

Initial result: failed.

## Failure Classification

- `INV-CANON-INDEX`: stale doc index/status disagreement and one missing historical index target.
- `INV-DOC-STATUS-HEADER`: current docs and audit docs missing the required four-line status header shape, plus invalid legacy status/date values.
- `INV-ALL-PRODUCTS-EMIT-DESCRIPTOR` / `INV-NO-ADHOC-MAIN`: RepoX still expected pre-canonical flat `dist/bin` wrapper paths and `tools/compat/tool_emit_descriptor` markers.
- `INV-OFFICIAL-PACKS-HAVE-COMPAT-MANIFEST`: RepoX still searched old `packs/` instead of canonical `content/packs/`.
- `INV-PACKS-MUST-DECLARE-CAPABILITIES`: `content/packs/blueprint/blueprints.default.m1/pack.json` lacked required capability/trust sidecars.
- `INV-NO-RAW-PATHS`: tracked fixtures contained old host absolute paths.
- `INV-WORLDGEN-LOCK-REQUIRED`: worldgen baseline and registry fingerprints were stale.
- `INV-REFINEMENT-BUDGETED` / `INV-NO-BLOCKING-WORLDGEN-IN-UI`: MW-4 probe failed because the committed pack lock artifact had stale PACK-COMPAT-2 metadata.
- `INV-IDENTITY-FINGERPRINT`: retained identity fingerprint artifact was stale after canon index changes.
- `tools/dist/runtime_compile_helper.py`: stale helper route repaired to `tools/release/dist/runtime_compile_helper.py`.
- Smoke CTest later exposed stale current test/tool routes:
  - `tools/worldgen_offline` -> `tools/domain/worldgen/offline`
  - `tools/playtest` -> `tools/test/playtest`
  - retired `tools/inspect/*_inspector.py` test helper paths.

## Fixes Made

- Updated RepoX STRICT to validate canonical release bundle paths under `dist/v0.0.0-mock/win64/dominium/bin`.
- Updated descriptor wrapper validation to accept current `tools.validators.compatibility` descriptor emission and importlib product delegation.
- Retargeted official pack discovery to `content/packs/` while preserving `tools/xstack/testdata/packs/` fixtures.
- Retargeted release generation from old `data/registries` and old `tools/dist/runtime_compile_helper.py` paths to canonical `contracts/registry` and `tools/release/dist`.
- Added missing `pack.capabilities.json` and `pack.trust.json` sidecars for `content/packs/blueprint/blueprints.default.m1`.
- Regenerated the MVP profile bundle and pack lock with current PACK-COMPAT-2 metadata.
- Regenerated the worldgen baseline snapshot and verify report JSON with the existing worldgen tools.
- Recomputed `contracts/registry/worldgen_lock_registry.json` deterministic fingerprint with the existing worldgen lock helper.
- Refreshed `docs/archive/audit/identity_fingerprint.json` using `scripts/dev/identity_fingerprint_lib.py`.
- Normalized raw fixture paths to repo-relative paths in the affected tracked fixtures.
- Added required status headers to current docs/audits and fixed the canon index entry for `docs/repo/final_repository_structure.md`.
- Repaired current smoke-test path expectations for world definition, playtest, interaction, and signal proof helpers.
- Added deterministic test-only inspectors under `tools/test/inspect/` for interaction and signal save/replay smoke checks.
- Repaired the active pack audit helper to scan canonical `content/packs/**/pack_manifest.json`, write retained evidence to `docs/archive/audit/PACK_AUDIT.txt`, and call `tools/package/pack/pack_validate.py`.
- Fixed `tools/package/pack/pack_validate.py` so direct script execution resolves the repo root and canonical distribution/fabrication helper paths.

## Generated Files

- Regenerated by `tools/release/dist/tool_assemble_dist_tree.py`:
  - `archive/generated/audit/dist_content_audit.json`
  - `docs/archive/audit/DIST_CONTENT_AUDIT.md`
- Regenerated by `tools/domain/worldgen/tool_generate_worldgen_baseline.py`:
  - `tests/fixtures/baselines/worldgen/baseline_worldgen_snapshot.json`
- Regenerated by `tools/domain/worldgen/tool_verify_worldgen_lock.py`:
  - `archive/generated/audit/worldgen_lock_verify.json`
- Regenerated by `tools/audit/pack_audit.py`:
  - `docs/archive/audit/PACK_AUDIT.txt`
- Refreshed by root layout validators:
  - `tools/migration/root_inventory.json`
  - `tools/migration/root_move_map.json`
- Local evidence under `.dominium.local/repox-strict-debt-01/` was not committed.

## Helper Route Decision

The missing `tools/dist/runtime_compile_helper.py` reference was stale. The helper is release distribution assembly support, so the canonical owner is `tools/release/dist/runtime_compile_helper.py`; no `tools/dist/` root was recreated.

## Validation Results

```text
py -3 .aide/scripts/aide_lite.py doctor
```

Result: PASS.

```text
py -3 .aide/scripts/aide_lite.py validate
```

Result: PASS, with the existing review packet reference warning.

```text
python -m py_compile ...
```

Result: PASS for touched Python tools/tests.

```text
python scripts/ci/check_repox_rules.py --repo-root . --profile STRICT --proof-manifest-out .dominium.local/repox-strict-debt-01/repox_proof_manifest_post_smoke_fix.json --profile-out .dominium.local/repox-strict-debt-01/repox_profile_post_smoke_fix.json
```

Result: PASS (`RepoX governance rules OK.`).

```text
python tools/validators/check_repo_layout.py --repo-root . --strict --no-write
python tools/validators/check_root_allowlist.py --repo-root . --strict
python tools/validators/repo/check_bad_root_absence.py --repo-root .
python tools/validators/repo/check_no_src_source_dirs.py --repo-root .
python tools/validators/repo/check_forbidden_root_names.py --repo-root .
python tools/validators/repo/check_docs_taxonomy.py --repo-root . --strict
python tools/validators/repo/check_tools_taxonomy.py --repo-root . --strict
python tools/validators/repo/check_content_layout.py --repo-root . --strict
```

Result: PASS. Forbidden-name check remains `PASS_WITH_WARNINGS` for archived/historical material only.

```text
python scripts/verify_docs_sanity.py --repo-root .
python scripts/verify_build_target_boundaries.py --repo-root .
python scripts/verify_includes_sanity.py --repo-root .
python scripts/verify_ui_shell_purity.py --repo-root .
python scripts/verify_abi_boundaries.py --repo-root .
```

Result: PASS.

```text
python scripts/dev/testx_proof_engine.py --repo-root . --suite testx_fast --dry-run --summary-json .dominium.local/repox-strict-debt-01/testx_fast_summary_final.json --summary-md .dominium.local/repox-strict-debt-01/testx_fast_summary_final.md --run-meta-json .dominium.local/repox-strict-debt-01/testx_fast_run_meta_final.json
python tests/invariant/test_survival_no_console.py --repo-root .
python tests/invariant/test_survival_no_freecam.py --repo-root .
python tests/invariant/test_survival_diegetic_only.py --repo-root .
```

Result: PASS.

```text
python tools/domain/worldgen/tool_verify_worldgen_lock.py --repo-root . --output-path .dominium.local/repox-strict-debt-01/worldgen_lock_verify_validation.json --doc-path .dominium.local/repox-strict-debt-01/WORLDGEN_LOCK_VERIFY_validation.md
```

Result: PASS (`matches_snapshot: true`).

```text
python tools/package/pack/pack_validate.py --repo-root . --pack-root content/packs/core/org.dominium.core.units --format json
python tests/tools/tools_pack_validate_tests.py --repo-root .
python tools/audit/pack_audit.py
```

Result: PASS for the canonical sample pack validator and focused pack validator test. `PACK_AUDIT.txt` remains retained archive evidence and records 14 pack-reference/FAB failures in current content packs as package/content debt; those failures are not RepoX STRICT blockers.

```text
cmake --preset verify
cmake --build --preset verify --target ALL_BUILD
ctest --preset verify -L smoke --output-on-failure
```

Result: PASS. Smoke CTest passed 57/57 after stale helper paths were repaired. The build emitted known duplicate-symbol linker warnings already documented by the prior remediation prompt.

Full CTest: not run in this pass; smoke CTest was run.

## Final RepoX STRICT Status

PASS.

## Remaining Blockers

None for RepoX STRICT in this pass. Remaining non-blocking debt:

- `docs/archive/audit/PACK_AUDIT.txt` records 14 current pack-reference/FAB validation failures as retained package audit evidence.
- Full CTest was not run.

## Feature Work Status

YES for work that only required RepoX STRICT, canonical path proof, CMake verify build, and smoke CTest. FULL-gate work remains limited until full CTest is run.

## Ending State

- Ending commit: the commit containing this audit. The exact hash is reported in the final operator summary after commit creation.
- Commit message target: `fix(repo): repair repox strict debt`
