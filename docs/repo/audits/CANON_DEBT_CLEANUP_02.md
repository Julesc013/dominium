Status: DERIVED
Last Reviewed: 2026-05-20
Supersedes: none
Superseded By: none

# CANON-DEBT-CLEANUP-02

Status: PASS_WITH_WARNINGS
Date: 2026-05-19
Starting branch: main
Starting commit: 2717d898ca63348ba825c7758f914fce05891f31
Starting origin/main: 2717d898ca63348ba825c7758f914fce05891f31

## Scope

This wave applies the bounded canonical debt cleanup requested after
`RUNTIME-NAME-01`. The first execution block is limited to the active source
spine and proof-critical cleanup:

- `GAME-RULE-01`
- `SCHEMA-CANON-01` where routes are deterministic
- `CONTENT-PACK-CANON-01` and `PACK-AUTHORITY-02` where package identity is not
  changed silently
- `APPS-THIN-01` as audit plus clear ownership repairs only

Later phases (`TOOLS-FOLD-01`, `DOCS-CANON-01`, `AIDE-ARCHIVE-SCAN-01`, and
validator hardening beyond touched areas) remain gated on the first block being
green.

## Guardrails

- No deletion.
- Use tracked moves for tracked files.
- Do not manually rewrite generated or historical registries when a generator
  refresh is the correct route.
- Do not change package IDs, profile IDs, release IDs, schema identity, ABI
  meaning, save/replay meaning, or policy semantics.
- Stop rather than guess when a route depends on semantic interpretation not
  proven by content, includes/imports, build wiring, tests, or current docs.

## Baseline

- Branch: `main`
- HEAD: `2717d898ca63348ba825c7758f914fce05891f31`
- origin/main: `2717d898ca63348ba825c7758f914fce05891f31`
- Preflight generated task context for `CANON-DEBT-CLEANUP-02`.
- Preflight refreshed root inventory metadata to the current HEAD.

## Baseline Validation

| Command | Result | Notes |
| --- | --- | --- |
| `py -3 .aide/scripts/aide_lite.py doctor` | PASS | Run before this audit note. |
| `py -3 .aide/scripts/aide_lite.py validate` | PASS_WITH_WARNINGS | Existing review packet ref and stale repo map warnings. |
| `python tools/validators/check_repo_layout.py --repo-root . --strict` | PASS | Existing root exceptions only. |
| `python tools/validators/check_root_allowlist.py --repo-root . --strict` | PASS | Existing root exceptions only. |
| `python tools/validators/check_distribution_layout.py --repo-root . --strict` | PASS | No warnings. |
| `python tools/validators/check_component_matrices.py --repo-root . --strict` | PASS | No warnings. |
| `python tools/validators/repo/check_bad_root_absence.py --repo-root . --json` | PASS | 0 tracked bad-root files. |
| `python tools/validators/repo/check_no_src_source_dirs.py --repo-root .` | PASS_WITH_WARNINGS | Historical/archive findings only. |
| `python tools/validators/repo/check_forbidden_root_names.py --repo-root .` | PASS_WITH_WARNINGS | Historical/archive findings plus known warnings. |

## Phase Notes

### GAME-RULE-01

Status: APPLIED, pending build/test proof.

Moves:

- 157 tracked implementation/local-header files moved out of `game/rules/`.
- 88 tracked public headers moved out of `game/include/dominium/rules/`.
- Source implementations now live under `game/domain/<area>/`, with existing
  shared rule machinery left under `game/rule/`.
- Public headers now use direct `game/include/dominium/<area>/` include roots,
  matching existing include convention.

Route notes:

- `agents` source -> `game/domain/agent/`; public headers -> `dominium/agents/`.
- `fab` source -> `game/domain/fabrication/`; public headers already used
  `dominium/fab/`.
- `physical` source -> `game/domain/physics/`; public headers already used
  `dominium/physical/`.
- Top-level `needs_constraints` moved to survival.
- Top-level `reproduction_rules` moved to life.
- Governance remained a domain route (`game/domain/governance/`) rather than a
  law-substrate route because files model governance systems, jurisdiction,
  legitimacy, policy models, and work tasks.

Reference handling:

- Active source, tests, validators, scripts, and current schema docs were
  updated from `dominium/rules/...` to direct `dominium/<area>/...` includes.
- Historical/audit/refactor references were intentionally not rewritten.
- `contracts/registry/architecture/module_registry*.json` remains stale
  generated registry evidence and must be regenerated by the owning generator;
  a partial manual rewrite was not retained.

### SCHEMA-CANON-01

Status: APPLIED as deterministic subset, pending build/test proof.

Moves:

- `contracts/schema/chem/` -> `contracts/schema/domain/chemistry/`
- `contracts/schema/fluid/` -> `contracts/schema/domain/fluids/`
- `contracts/schema/geo/` -> `contracts/schema/domain/geology/`
- `contracts/schema/civ/` and `contracts/schema/civilisation/` ->
  `contracts/schema/domain/civilization/`
- `contracts/schema/net/` -> `contracts/schema/runtime/network/`
- `contracts/schema/packs/` -> `contracts/schema/package/`
- `contracts/schema/tools/` -> `contracts/schema/tool/`
- `contracts/schema/validator/` -> `contracts/schema/validation/`
- `contracts/schema/control/` -> `contracts/schema/runtime/control/`

Reference handling:

- Active source, tests, current docs, planning contracts, validators, and
  RepoX rule paths were updated to the routed schema locations.
- Generated or historical registry evidence was not manually rewritten.
- `contracts/registry/architecture/module_registry*.json` still needs an
  owning generator refresh.

Deferred schema buckets:

- `contracts/schema/core/`
- `contracts/schema/lib/`
- `contracts/schema/compatibility/`
- `contracts/schema/runtime/diagnostics/`
- `contracts/schema/domain/modeling/`
- `contracts/schema/package/modding/`
- `contracts/schema/runtime/render/`
- `contracts/schema/validation/specs/`
- remaining already-canonical-or-ambiguous category roots requiring a focused
  `SCHEMA-CANON-02` pass

### CONTENT-PACK-CANON-01 / PACK-AUTHORITY-02

Status: AUDITED, no identity-changing moves applied.

Findings:

- `contracts/package/packs/` contains only `README.md`.
- The README explicitly reserves the path for pack contract definitions and
  states authored pack payloads and pack-local manifests live under
  `content/packs/`.
- `content/packs/` already follows a mostly category-plus-pack layout, but it
  still mixes top-level helper files, compatibility helper code, and multiple
  category names. This needs a focused package-law-aware pass before pack IDs
  or internal pack layout are changed.
- `content/domains/game/core/` remains tracked. Although it is a small
  deterministic content wrapper, protected planning surfaces still name it as a
  preserved foundation, so it was classified for follow-up instead of moved in
  this wave.

### APPS-THIN-01

Status: AUDITED, no blind moves applied.

Retained for focused follow-up:

- `apps/client/local_server/`
- `apps/client/model/`
- `apps/client/presentation/`
- `apps/server/authority/`
- `apps/server/persistence/`
- `apps/server/shard/`
- `apps/launcher/lifecycle/`
- `apps/setup/lifecycle/`

Reason:

- These paths include product glue, runtime-like adapters, server-specific
  sharding/persistence, client presentation, and lifecycle code. Their correct
  destinations require file-level API and build ownership review. Moving them
  mechanically would risk changing app/runtime boundaries silently.

## In-Progress Validation

| Command | Result | Notes |
| --- | --- | --- |
| `git ls-files game/rules game/include/dominium/rules ...` | PASS | 0 tracked files under retired game/schema paths checked in this wave. |
| `python scripts/verify_build_target_boundaries.py` | PASS | Build boundary checks passed after game/schema routing. |
| `python scripts/verify_docs_sanity.py --repo-root .` | PASS | Docs sanity OK. |
| `git diff --check` | FIXED | Initial extra EOF blank lines in schema docs were removed mechanically. |

## Final Validation

| Command | Result | Notes |
| --- | --- | --- |
| `py -3 .aide/scripts/aide_lite.py validate` | PASS_WITH_WARNINGS | Existing review-packet and stale repo-map warnings remain. |
| `python tools/validators/check_repo_layout.py --repo-root . --strict` | PASS | Existing exceptions only. |
| `python tools/validators/check_root_allowlist.py --repo-root . --strict` | PASS | Existing exceptions only. |
| `python tools/validators/check_distribution_layout.py --repo-root . --strict` | PASS | No warnings. |
| `python tools/validators/check_component_matrices.py --repo-root . --strict` | PASS | No warnings. |
| `python tools/validators/repo/check_bad_root_absence.py --repo-root . --json` | PASS | 0 tracked bad-root files. |
| `python tools/validators/repo/check_no_src_source_dirs.py --repo-root .` | PASS_WITH_WARNINGS | Archive/historical findings only. |
| `python tools/validators/repo/check_forbidden_root_names.py --repo-root .` | PASS_WITH_WARNINGS | Known warning inventory; blocker count 0. |
| `python scripts/verify_build_target_boundaries.py` | PASS | Active boundary paths updated. |
| `python scripts/verify_docs_sanity.py --repo-root .` | PASS | Docs sanity OK. |
| `python scripts/verify_includes_sanity.py --repo-root .` | PASS | Include sanity OK. |
| `python scripts/verify_ui_shell_purity.py --repo-root .` | PASS | UI shell purity OK. |
| `python scripts/verify_abi_boundaries.py --repo-root .` | PASS | ABI boundaries OK. |
| `python scripts/verify_cmake_no_global_includes.py --repo-root .` | PASS | No global include drift. |
| `cmake --preset verify` | PASS | Existing SDL/PkgConfig warnings only. |
| `cmake --build --preset verify --target ALL_BUILD` | PASS_WITH_WARNINGS | Existing duplicate-symbol linker warnings remain. |
| `ctest --preset verify -L smoke --output-on-failure --timeout 300` | PASS | 57/57 tests passed. |
| focused moved-code CTest lane | PASS | 15/15 moved-code tests passed. |
| broad rule/profile CTest lane | FAIL_EXISTING | Six TestX survival/profile checks still expect `data/registries/law_profiles.json`. |
| `ctest --preset verify -R inv_repox_rules --output-on-failure --timeout 300` | FAIL_EXISTING | RepoX still contains broad pre-canon stale paths and a Windows console encoding crash while printing violations. |
| `git diff --check` | PASS | Whitespace clean. |

## RepoX Disposition

The focused RepoX lane was repaired past the immediate worldgen-lock crash:

- worldgen registry sources now resolve from `contracts/registry/`;
- worldgen baseline seed/snapshot paths now resolve from
  `tests/fixtures/baselines/worldgen/`;
- worldgen verification JSON now resolves from
  `archive/generated/audit/worldgen_lock_verify.json`;
- deterministic Sol anchor object IDs resolve again, including
  `sol.planet.earth`.

RepoX still fails because the validator itself encodes a large pre-canon path
surface, including `data/registries`, `contracts/schemas`, `game/domains`,
`runtime/appshell`, `runtime/shell/appcore`, `docs/worldgen`, and old
compat/appshell locations. That is not safe to finish as a side effect of this
source-spine pass; it needs a dedicated `REPOX-CANON-PATHS-01` repair with
UTF-8 output handling on Windows.

## Remaining Gated Phases

- `REPOX-CANON-PATHS-01`
- TestX survival/profile registry path repair
- `SCHEMA-CANON-02`
- package-law-aware `CONTENT-PACK-CANON-01`
- file-level `APPS-THIN-01`
- `TOOLS-FOLD-01`
- `DOCS-CANON-01`
- `AIDE-ARCHIVE-SCAN-01`
- broad `VALIDATOR-HARDEN-01`
- `FULL-PROOF-01`
