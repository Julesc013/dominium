Status: DERIVED
Last Reviewed: 2026-04-02
Supersedes: none
Superseded By: none
Stability: provisional
Future Series: Λ, Σ, Φ, Υ, Ζ
Binding Sources: `docs/canon/constitution_v1.md`, `docs/canon/glossary_v1.md`, `AGENTS.md`, `docs/planning/POST_PI_EXECUTION_PLAN.md`, `data/planning/final_prompt_inventory.json`, `data/planning/dependency_graph_post_pi.json`

# Merged Program State

## 1. Current Position

The repository is now in the merged post-Ω/Ξ/Π and post-P-0 through P-4 planning state.

Completed planning stack:

- `Ω`
- `Ξ`
- `Π`
- `P-0 Snapshot Intake and Normalization`
- `P-1 Reality Extraction`
- `P-2 Blueprint Reconciliation`
- `P-3 Foundation Readiness`
- `P-4 Final Prompt Synthesis`
- `0-0 Planning Hardening`

The current planning state is stable enough to begin execution-grade semantic work without reopening doctrine.

## 2. Next Executable Step

The next executable prompt is:

- `Λ-0 UNIVERSAL_REALITY_FRAMEWORK-0`

The current next review checkpoint is:

- `RG-SEM-0 semantic ownership review`

That review gate must run before `Λ-6 CROSS_DOMAIN_BRIDGES-0`.

## 3. Current Recommended Sequence

The active recommended order is:

1. `Λ-0` through `Λ-5`
2. `RG-SEM-0 semantic ownership review`
3. `Λ-6`
4. `CHK-ΛΣΦ-0`
5. `Σ-A` (`Σ-0` through `Σ-2`)
6. `Σ-B` (`Σ-3` through `Σ-5`)
7. `CHK-ΣΦ-0`
8. `Φ grounding` (`Φ-0` through `Φ-6`)
9. `RG-PHI-0`
10. `Υ consolidation`
11. `deep Φ`
12. `late-gated Ζ`

This is the live continuity order.
Later prompts should not collapse it into a flatter sequence.

## 4. Doctrine Locks

The following locks should now be treated as stable enough to build on:

- `constitutional_lock`: `docs/canon/constitution_v1.md` remains the binding contract.
- `glossary_lock`: `docs/canon/glossary_v1.md` remains the binding vocabulary floor.
- `agent_control_lock`: `AGENTS.md` remains the binding task and validation discipline.
- `intake_authority_lock`: P-0 intake protocol and authority order remain the active planning intake law.
- `repo_reality_lock`: P-1 reality extraction is the current grounded map of what exists.
- `extend_over_replace_lock`: P-2 keep / extend / merge / replace / quarantine decisions remain binding for later planning.
- `readiness_lock`: P-3 readiness statuses remain the default execution gating until new repo evidence proves otherwise.
- `execution_order_lock`: P-4 sequence and dependency graph remain the active execution program.
- `version_identity_split_lock`: Υ must preserve explicit identity, compatibility, and build/release distinctions rather than collapsing them into version strings.

These are planning locks, not excuses to stop re-reading the repo.
If later repo reality changes materially, the change must be recorded explicitly rather than silently overriding these locks.

## 5. Major Unresolved Quarantine Items

The active unresolved quarantine set is:

- `field` versus `fields`
- `schema` versus `schemas`
- `packs` versus `data/packs`
- generated echoes under `build`, `artifacts`, `.xstack_cache`, and `run_meta`
- historical roots under `legacy`, `attic/src_quarantine`, and `quarantine`

These still matter because:

- `Λ-6` depends on the ownership review around them
- `Φ-12` and `Φ-14` depend on them
- `Υ` publication, compatibility, and identity work must not silently choose winners among them

## 6. Extension-Over-Replacement Zones

The current highest-priority extension-over-replacement zones are:

- `client`, `server`, `launcher`, `setup` as stable product anchors
- `engine`, `game`, and `game/content/core` as the strongest runtime foundation
- `app`, `compat`, `control`, `core`, `net`, `process`, `server/runtime`, and `server/persistence` as the real runtime substrate cluster
- `release`, `repo`, `updates`, `data/architecture`, `data/registries`, and `tools/xstack` as the control-plane backbone
- `tools/controlx`, `tools/release`, `tools/distribution`, `tools/compatx`, and `tools/securex` as operator and trust tooling to consolidate rather than replace
- `worldgen`, `geo`, `reality`, `materials`, `logic`, `signals`, `system`, `universe`, `epistemics`, `diegetics`, `infrastructure`, and `machines` as semantic roots to formalize rather than reinvent
- `AGENTS.md`, `docs/planning`, and the existing governance docs as surfaces to normalize rather than duplicate
- the build and preset matrix under `CMakeLists.txt`, `CMakePresets.json`, `cmake/**`, `.github/workflows/ci.yml`, and `scripts/**`

The main non-extension exception remains:

- `runtime/` must not be treated as the obvious canonical orchestrator home

## 7. Important Invalidated Assumptions

The most important already-invalidated assumptions are:

- runtime and service foundations are not absent; they already exist in distributed form
- semantic/domain embodiment is not future-only; it already exists in meaningful repo roots
- release and control-plane work is not mostly theoretical; it already has strong live substrate
- governance mirroring is not absent; the problem is fragmentation, not nonexistence
- duplicate-shadow risk is not legacy-only; it exists in live pack, field, schema, and generated surfaces
- the repo cannot be modeled safely as a tiny handful of source roots anymore
- pack and schema ownership are not single-root truths right now

## 8. Relationship Between The Current Doctrine Layers

### 8.1 Constitutional Doctrine

Constitution, glossary, and `AGENTS.md` remain the top binding layer.
They define what later work is allowed to mean and how it must be verified.

### 8.2 Reality And Semantic Doctrine

`Λ` is the next executable series because the repo already embodies semantic and world structure that now needs formalization.
The semantic layer is not theoretical.
It is the bridge between constitutional meaning and repo-grounded execution.

### 8.3 Platform, Runtime, And Release Doctrine

`Φ` and `Υ` are not clean-room architecture tracks.
They are extraction and consolidation tracks over a repo that already contains real runtime, release, compat, and control-plane surfaces.

### 8.4 Repo-Grounded Execution Planning

P-0 through P-4 converted doctrine and repo evidence into the current execution program.
`0-0` hardens that program so later prompts inherit:

- a merged continuity state
- an explicit anti-reinvention ledger
- explicit gates and exit proofs
- a stable chat handoff policy

## 9. Practical Orientation For Future Planning

If a future chat needs one-page orientation, it should use this document plus:

- `data/planning/merged_series_state.json`
- `docs/planning/GATES_AND_PROOFS.md`
- `docs/planning/EXTEND_NOT_REPLACE_LEDGER.md`
- `docs/planning/POST_PI_EXECUTION_PLAN.md`

That is the current minimal continuity packet for the program.
