# Platform API (dom_plat.h)

TODO: move the platform contract from inline headers into this document. The new `include/dominium/dom_plat.h` is the authoritative entry point; implementations live under `src/platform/*` and must stay behind the platform layer (no direct OS includes elsewhere).
