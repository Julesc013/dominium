# AIDE Path Aliases

Path aliases are temporary compatibility evidence. They are not identity, must retire, and are disabled by default. Active aliases may only be introduced after an approved move map and explicit validation.
