# Renderer API (dom_rend.h)

TODO: consolidate renderer ABI notes here. `include/dominium/dom_rend.h` currently forwards to the internal API while hardware backends are being staged; keep renderer access behind dom_plat and avoid leaking platform-specific headers into products.
