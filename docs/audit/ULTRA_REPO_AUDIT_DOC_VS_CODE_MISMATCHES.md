# ULTRA REPO AUDIT DOC VS CODE MISMATCHES

Implementation evidence outranked prose when the two disagreed.

| Mismatch | Doc Claim | Code Evidence | Type | Recommended Treatment |
| --- | --- | --- | --- | --- |
| MISMATCH-001 | docs/audit/CAP_NEG1_RETRO_AUDIT.md says `src/server/server_main.py` is the authoritative server CLI surface. | The live file is server/server_main.py, and direct invocation is currently blocked because REPO_ROOT_HINT points two directories above the repo root. | path_and_readiness_drift | Refresh the doc path and narrow the claim until direct Python server startup is fixed. |
| MISMATCH-002 | docs/audit/CAP_NEG2_RETRO_AUDIT.md references src/server/net/loopback_transport.py and src/client/local_server/local_server_controller.py as live implementation anchors. | The live implementation roots are server/net/loopback_transport.py and client/local_server/local_server_controller.py; many derived docs still retain legacy src/ prefixes. | structural_drift | Refresh derived audit/docs mirrors to current root names without rebinding canonical ownership. |
| MISMATCH-003 | docs/audit/UX0_RETRO_AUDIT.md states that existing runtime/bootstrap is already portable across CLI, GUI, and headless entrypoints. | launcher/gui/launcher_gui_stub.c and launcher/tui/launcher_tui_stub.c are explicit stubs; setup.exe smoke also self-reported a stub status. | maturity_drift | Restate portability as CLI/headless verified, Python/AppShell TUI usable, native launcher GUI/TUI still partial. |
| MISMATCH-004 | docs/audit/CAP_NEG1_RETRO_AUDIT.md describes tools/mvp/runtime_entry.py as the practical MVP client/server bootstrap surface. | The audited local-singleplayer invocation fell through to the AppShell/TUI surface instead of a clear playable session launch. | behavioral_drift | Either harden runtime_entry into the canonical baseline launcher or downgrade the doc claim and point users at the stronger live shell/controller path. |
| MISMATCH-005 | Multiple derived docs cite `src/...` file paths and older root layouts as if they were the active live tree. | The live repository uses top-level roots such as client/, server/, launcher/, setup/, appshell/, schema/, and specs/reality/ without the old src/ container. | naming_drift | Refresh path mirrors in derived docs and reports during the next documentation maintenance pass. |
| MISMATCH-006 | Session-pipeline doctrine and reports generally frame session boot as an available governed path once a SessionSpec exists. | tools/xstack/sessionx/runner.py assumes repo_root/saves/<save_id> even when session_create was told to write elsewhere, so the practical boot path is narrower than the docs imply. | integration_drift | Document the current canonical saves-root requirement or fix runner.py to honor the session artifact location contract. |
