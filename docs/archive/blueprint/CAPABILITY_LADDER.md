Status: DERIVED
Last Reviewed: 2026-03-31
Supersedes: none
Superseded By: none
Stability: provisional
Future Series: PI
Replacement Target: snapshot-anchored capability ladder after real repository mapping

# Capability Ladder

## Ladder Overview

Higher levels inherit every lower-level invariant and dependency.

## Level 0 - Frozen MVP foundations

- `Compatibility and trust`: required series `OMEGA`; status `ready_now`.
- `Deterministic truth paths`: required series `OMEGA`; status `ready_now`.
- `Distribution and archive`: required series `OMEGA, UPSILON`; status `foundation_ready`.
- `Frozen MVP foundations`: required series `OMEGA`; status `foundation_ready`.

## Level 1 - Operator and governance

- `Deterministic maintenance workflows`: required series `XI, SIGMA`; status `foundation_ready`.
- `Operator and governance surface`: required series `XI, SIGMA`; status `foundation_ready`.

## Level 2 - Componentized runtime

- `Asset and shader pipeline`: required series `PHI, UPSILON`; status `foundation_ready`.
- `Framegraph and render scheduling`: required series `PHI`; status `requires_new_foundation`.
- `Lifecycle manager`: required series `PHI`; status `requires_new_foundation`.
- `Module loader`: required series `PHI`; status `requires_new_foundation`.
- `Render device abstraction`: required series `PHI`; status `requires_new_foundation`.
- `Service registry`: required series `PHI`; status `requires_new_foundation`.
- `State export and import`: required series `PHI, UPSILON`; status `requires_new_foundation`.

## Level 3 - Controlled live operations

- `Canary and staged rollouts`: required series `UPSILON, ZETA`; status `requires_new_foundation`.
- `Live backend swap`: required series `PHI, ZETA`; status `requires_new_foundation`.
- `Live pack mount`: required series `UPSILON, ZETA`; status `requires_new_foundation`.
- `Non-blocking saves`: required series `PHI, ZETA`; status `requires_new_foundation`.
- `Shadow service startup`: required series `PHI, UPSILON, ZETA`; status `requires_new_foundation`.

## Level 4 - Distributed live operations

- `Authority handoff`: required series `PHI, UPSILON, ZETA`; status `requires_new_foundation`.
- `Cluster failover`: required series `PHI, UPSILON, ZETA`; status `requires_new_foundation`.
- `Deterministic replication`: required series `PHI, UPSILON, ZETA`; status `requires_new_foundation`.
- `Shard relocation`: required series `PHI, UPSILON, ZETA`; status `unrealistic_currently`.

## Level 5 - Extreme pipe dreams

- `Deterministic cluster-of-clusters`: required series `PHI, UPSILON, ZETA`; status `unrealistic_currently`.
- `Live schema evolution`: required series `SIGMA, PHI, UPSILON, ZETA`; status `unrealistic_currently`.
- `Multi-backend mirrored rendering`: required series `PHI, ZETA`; status `unrealistic_currently`.
- `Restartless core engine replacement`: required series `SIGMA, PHI, UPSILON, ZETA`; status `unrealistic_currently`.

