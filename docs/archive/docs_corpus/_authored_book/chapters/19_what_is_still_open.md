## 19. What Is Still Open

The open questions are not a defect; they are the reason the archive exists as evidence instead of doctrine.

Docs-corpus and conversation dockets identify user decisions, future queue decisions, and source verification needs.

The current material supports that picture in several ways. Foundation Lock is the repository decision gate that determines whether Dominium can leave the Foundation Lock queue and begin a first narrow governed product slice. Decision items are review prompts. The default disposition is defer unless a later explicit task opens the scope. Scope: index of frozen and evolving constitutional surfaces. This index is the navigation hub for stability surfaces. "FROZEN" means the.

A second current-thread is the project’s insistence that roles remain explicit. Document defines a contract that must not change lightly. "EVOLVING" means the. Schemas are the authoritative data-shape contracts. Start here. 'FOUNDATION-CLOSEOUT-02' reran the Foundation Lock gate after 'FOUNDATION-REPAIR-DEPENDENCY-DIRECTION-01' and closed the lock with warnings. 'PORTABILITY-ARCH-POLICY-02' has since completed the first post-closeout hardening follow-up. All required files for the 15 Foundation Lock layers are present.

Current documentation also narrows the interpretation of the theme. Current partially aligned to the Constitutional Architecture and release-freeze documentation set. The simulation kernel is the source of truth; perception layers are derived. Objective snapshots record authoritative state; subjective snapshots record views. Determinism and replay-first design are mandatory.

The current repo-backed reading is therefore less about feature breadth than about preserving the shape of authority while the project grows. Document is binding but still expected to grow or sharpen. '': What disposition should be chosen for this unresolved claim: Future work must define the client's allowed dependency surface and whether it can access any simulation-adjacent prediction layer.? Safe now: archive reading, decision review, promotion review, reconciliation crosswalks, docs-only microtask preparation, and validators. Use: design intent, review backlog, decision and promotion candidates.

The archive contains many plausible answers, but a plausible answer is not the same as a repo decision.

The conversation corpus adds more texture. '': What disposition should be chosen for this unresolved claim: Future work must define the client's allowed dependency surface and whether it can access any simulation-adjacent prediction layer.? Safe now: archive reading, decision review, promotion review, reconciliation crosswalks, docs-only microtask preparation, and validators. What disposition should be chosen for this unresolved claim: Future work must define the client's allowed dependency surface and whether it can access any simulation-adjacent prediction layer.? The entire conversation is anchored by the requirement that Dominium must run deterministically. This means the same initial state and same inputs must produce the same state across machines, compilers, operating systems, and eventually retro and modern.

The strongest historical value is not that every claim is current, but that repeated concerns become visible. Use this page as the shortest path through the conversation-corpus adjudication surfaces. Verify latest queue. If unchanged, proceed with 'PRESENTATION-CONTRACT-01'; then draft 'UNIVERSE-EXPLORER-CONTRACT-01'. Calendars are pure renderers and never affect physics. Adopt base-16 spatial ladder from subnano to surface.

Across the chat-derived reports, the same theme often appears in product, architecture, and governance language. Store surfaces as sparse microgrids/chunks, not continuous dense arrays. Final support tiers are user-defined and must be preserved. This preservation package explains the chat, records decisions, tasks, artifacts, risks, and open questions, and exports a structured spec/aggregation packet for future merging. Which repo files supported the Dominium conclusion?

The archive also records how the project repeatedly returned to the same pressure points from different directions. The final state of the conversation is therefore: the architecture and implementation-roadmap discussion has been preserved, but the actual next work is elsewhere-especially the Path D advanced simulation conversation. Most important: actual repo structure, existing GPT-5.2 refactor plan contents, final Q formats, orientation representation, grid assumptions in current code, microsegment length, packing VM instruction set, bridge/tunnel ownership, and checksum rules. How are 'DOMINIUM RUN ROOT' and 'DOMINIUM HOME' finalized? Read file 01 first if you want to understand what happened. Read file 04 if you need tasks and decisions. Read file 05 if merging into a master Project Spec Book. Read file 07 before treating external support claims as implementation truth.

Several decisions are folded into this topic rather than standing apart from it. Foundation Lock is the repository decision gate that determines whether Dominium can leave the Foundation Lock queue and begin a first narrow governed product slice. Decision items are review prompts. The default disposition is defer unless a later explicit task opens the scope. '': What disposition should be chosen for this unresolved claim: Future work must define the client's allowed dependency surface and whether it can access any simulation-adjacent prediction layer.? Safe now: archive reading, decision review, promotion review, reconciliation crosswalks, docs-only microtask preparation, and validators.

Other decisions remain tentative because the source material records review posture rather than final promotion. Use: design intent, review backlog, decision and promotion candidates. Calendars are pure renderers and never affect physics. Adopt base-16 spatial ladder from subnano to surface. Store surfaces as sparse microgrids/chunks, not continuous dense arrays.

The decision pattern that matters most is the distinction between a design direction that has been preserved and a rule that has actually become binding. Final support tiers are user-defined and must be preserved. This preservation package explains the chat, records decisions, tasks, artifacts, risks, and open questions, and exports a structured spec/aggregation packet for future merging. Which repo files supported the Dominium conclusion? The final state of the conversation is therefore: the architecture and implementation-roadmap discussion has been preserved, but the actual next work is elsewhere-especially the Path D advanced simulation conversation.

The governing constraint is practical rather than cosmetic. Foundation Lock does not mean full CTest is green, every compatibility corpus exists, every provider is implemented, every runtime trust rule is enforced, Workbench UI exists, or broad feature work is open. Keep Queue B hardening planned but not a substitute for the blocker repair. 'FOUNDATION-CLOSEOUT-01' was blocked because dependency-direction strict reported '358' violations and '38' warnings. Items touching blocked queue scope must remain explanatory or deferred; no implementation scope is opened.

Those constraints matter because they prevent future work from using the book as an implementation shortcut. Current blocked queue scopes: 'broad workbench ui', 'gameplay', 'native gui', 'package runtime', 'provider runtime', 'release publication', 'renderer implementation', 'runtime module loader'. Do not open blocked queue scopes from old conversations. The current queue blocks these broad work areas. Docs-corpus outputs record them for navigation only. Use generated contradictions and promotion queues to plan later tasks.

The same constraints also protect the reader from treating a polished explanation as a license to bypass queue, contract, or validation gates. This queue records later review candidates. It does not apply any live-doc patch. It does not open blocked renderer, gameplay, provider runtime, package runtime, broad Workbench UI, native GUI, or release publication work. Do not promote archive claims without a later reviewed task. Any Wave 1 candidate before source text and target docs are inspected together.

The work ahead is to separate questions that can be answered by docs hygiene from questions that need queue scope or user authority.

For what is still open, the useful synthesis is not a catalogue of all preserved claims. It is the shape that remains after current authority, archive memory, and conversation evidence are read together. The recurring material around open decisions, user questions, future queue points to a system that wants broad expressive power, but only when that power can be routed through explicit authority, deterministic behavior, and reviewable evidence.

The archive is most useful here as a record of pressure. It shows where the project kept returning to the same concern, where earlier language was too broad, and where product ambition outpaced queue authority. That historical pressure should inform later review, but it should not erase the difference between a preserved idea and a binding rule.

In practical terms, this chapter asks future work to convert desire into a narrower claim before it changes the repo. A good follow-up would identify the exact target surface, describe the authority it relies on, name the validation evidence it will produce, and say which parts of the larger ambition remain outside scope.

The result is a conservative but useful reading posture: preserve the knowledge, keep the uncertainty visible, and use what is still open as a review topic rather than an implementation shortcut. That posture is what lets the project remember the full archive without letting the archive silently become the project.

This is especially important for open decisions, user questions, future queue, where many sources describe future usefulness but only some describe current permission. A reader should therefore separate three things while reading: what is binding now, what the archive helps explain, and what still needs a deliberate review task.

Seen that way, the chapter is not trying to settle every preserved claim. It is trying to make the relevant knowledge usable: enough synthesis to understand the topic, enough restraint to avoid overclaiming, and enough traceability for a reviewer to inspect the source notes when a later decision depends on them.

Where the sources remain unsettled, the uncertainty has to be carried forward rather than hidden. Continue documentation and evidence updates that keep remaining warnings and full-gate debt visible. Players spawn into manufactured robot bodies. Possible forms include bipeds, quadrupeds, spiders, tank-like frames, drones, heavy industrial bodies, aquatic bodies, and aerial bodies. The exact launch list is deferred. The following were not finished or intentionally deferred. The reason these were deferred is not that they are unimportant. They were deferred because the local playable baseline path is not yet reliable. Feature work before baseline proof would increase uncertainty and project risk.

Some conflicts are not disagreements about desire; they are disagreements about authority, timing, or proof. How can Dominium grow for years or decades without collapsing into special cases, silent semantic drift, contradictory abstractions, or unmaintainable simulation layers? Proper time matters for local clocks, aging, and relativistic effects, but making it the basis of civil calendars would create frame-dependent disagreements and log problems. It was rejected as authoritative calendar time. DDAP/DIL is proposed, not yet user-ratified. Codex prompts were generated; application status is unknown.

That distinction matters because a future reviewer can often preserve the ambition while still rejecting the implied implementation step. The main unresolved goals are backend audit, ACT serialization audit, civil/astronomical projection design, and verification of external platform facts. UNCERTAIN / UNVERIFIED The exact VM is unresolved. The most important caveat is that actual repo state is unknown. 'UNCERTAIN / UNVERIFIED:' Whether the project has similarly named files or modules is unknown.

The downstream effect is that future work has to account for more than the immediate feature request. 4. Conceptual artifacts produced in chat. These include proposed architectures, matrices, lane plans, Workbench modules, Interface Operating Layer, boot-to-replay MVP, and structured roadmaps. Tool Editor eventually self-hosts and edits itself/other tools. Dominium/Domino Architecture and Codex Prompt Roadmap - Conversation Reader. 6. Long-term live operations remain future work.

This book should help the reader recognize the question before trying to solve it.
