Status: CANONICAL
Last Reviewed: 2026-04-07
Supersedes: none
Superseded By: none
Stability: stable
Future Series: post-baseline AIDE roadmapping and extraction-admission review
Replacement Target: later post-baseline AIDE roadmap ordering artifact or explicit replacement checkpoint only
Binding Sources: `docs/xstack/CHECKPOINT_C_XSTACK_AIDE_CLOSURE.md`, `data/xstack/checkpoint_c_xstack_aide_closure.json`, `docs/xstack/CODEX_REPO_OPERATING_CONTRACT.md`, `data/xstack/codex_repo_operating_contract.json`, `docs/xstack/XSTACK_TO_AIDE_EXTRACTION_MAP.md`, `data/xstack/xstack_to_aide_extraction_map.json`, `docs/audit/ULTRA_REPO_AUDIT_PRODUCT_ASSEMBLY_PLAN.md`, `docs/audit/ULTRA_REPO_AUDIT_PLAYTEST_READINESS.md`, `docs/audit/ULTRA_REPO_AUDIT_GAPS_AND_TODOS.md`, `docs/planning/NEXT_EXECUTION_ORDER_POST_ZETA.md`

# Next Execution Order Post-XStack/AIDE

## Recommended Next Prompt

The recommended next XStack/AIDE prompt, after the canonical playable baseline exists, is:

- `AIDE_POST_BASELINE_ROADMAP-0`

## Should Any Further XStack/AIDE Prompt Run Before The Baseline?

- `no`

Before the playable baseline exists, no new XStack/AIDE prompt should compete with:

- canonical repo-local playtest-path hardening
- session create/boot/startup blocker resolution
- loopback-authoritative baseline assembly
- FAST validation plus a selected smoke/playtest slice on the assembled baseline

## How To Use The Existing X-Series During Baseline Assembly

Use the current X-series as a constraint packet, not as a parallel execution band.

- Use `X-7` as the repo-operating law for build, validation, playtest, session, directory-authority, task-sizing, and commit rules.
- Use `X-0` and `X-1` to keep scope, ownership, and mixed-surface cautions explicit while fixing baseline blockers.
- Use `X-2` through `X-6` as contract vocabulary only when baseline work needs task, evidence, review, policy, capability, or adapter language.
- Use `X-8` to avoid accidental extraction or migration when touching portable-looking but still Dominium-owned surfaces.

## Recommended Ordering

1. Finish the canonical playable baseline and its blockers using the baseline-first audit and operating-contract rules.
2. Prove one repeatable repo-local playtest path with the canonical bundle, lock, session template, and loopback-authoritative mode.
3. Re-run `FAST` validation and the agreed smoke slice against that baseline.
4. Only then execute `AIDE_POST_BASELINE_ROADMAP-0`.

## Why This Ordering Is Best

This ordering is best because:

- the XStack/AIDE narrowing-and-contract phase is already complete in its intended scope
- the repo still gets more immediate value from baseline hardening than from further extraction planning
- post-baseline reality will provide better evidence for what is truly extractable, what still needs bridges, and what must remain in Dominium
- it prevents the repo from mistaking contract freezes for permission to start migration, platformization, or a second competing architecture track

## Post-Baseline Entry Expectations

`AIDE_POST_BASELINE_ROADMAP-0` should not start until baseline evidence exists for:

- one canonical repo-local playable path
- session materialization on the canonical layout
- one repeatable start or status or save-load-resume slice
- a fresh successful `FAST` validation run
- honest confirmation of which `X-8` extraction prerequisites are now satisfied versus still deferred
