# SR-1 Status: Kernel Skeleton

- Adds TLV core, contract schemas, audit model, SPLAT registry, and minimal kernel API.
- Adds `dominium-setup2` frontend for validation/run orchestration only.
- Adds setup2 kernel tests (TLV, validation, audit emission, SPLAT determinism).
- No installer behavior changes; no execution/transaction engine yet.
